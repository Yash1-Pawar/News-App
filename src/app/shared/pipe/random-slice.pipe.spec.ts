import { RandomSlicePipe } from './random-slice.pipe';

describe('RandomSlicePipe', () => {
  it('create an instance', () => {
    const pipe = new RandomSlicePipe();
    expect(pipe).toBeTruthy();
  });
});
