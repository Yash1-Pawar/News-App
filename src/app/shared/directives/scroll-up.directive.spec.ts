import { ScrollUpDirective } from './scroll-up.directive';

describe('ScrollUpDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollUpDirective();
    expect(directive).toBeTruthy();
  });
});
